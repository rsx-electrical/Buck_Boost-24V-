# Workflow
For every commit where 4_switch_Buck_Boost is changed a .zip file copy of the 4_switch_Buck_Boost must be saved in the History Folder.
Make sure the changes will appear in chronological order so have the names of the .zip file as `<date/time>_<name>_<brief_description_of_changes>`. 
